# k8s/mysql-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard   # 👈 مهم جدًا لـMinikube
  resources:
    requests:
      storage: 1Gi